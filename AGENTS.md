# AGENTS.md

本リポジトリで作業を行う AI コーディングアシスタント（エージェント）向けの開発ガイドラインおよびプロジェクト規約です。

---

## 1. プロジェクト概要

- **リポジトリ名**: `VRMEditorTools`
- **目的**: ブラウザ上でVRMモデル（0.x / 1.0）の読み込み、表示、テクスチャ変更、ブレンドシェイプ（表情）の確認・一括撮影・ダウンロード、3面図保存などを行うWebアプリケーション。
- **リポジトリ構成**:
  - `app/`: Vue.js によるフロントエンドアプリケーション本体
  - `README.md`: ユーザー向けドキュメント
  - `AGENTS.md`: エージェント向け開発ルール・ガイドライン（本ファイル）

---

## 2. 作業ディレクトリに関する最重要規則

> [!IMPORTANT]
> アプリケーションのコードおよび設定ファイルはすべて `app/` 配下に存在します。
> `npm` コマンド（`npm run serve`, `npm run build`, `npm run lint`, `npm install` など）を実行する際は、**必ずカレントディレクトリを `app/` に指定して実行してください。**

- 例: `cd /Volumes/SSD/work/cecil-tools/VRMEditorTools/app && npm run build`
  （※ツール実行時は `Cwd: .../app` を指定）

---

## 3. 技術スタック

- **Node.js**: `v24.12.0` 推奨
  - ※Vue CLI の実行には `NODE_OPTIONS='--openssl-legacy-provider'` が必要（`package.json` の各スクリプトに組み込み済み）。
- **フレームワーク**: Vue.js 2.6.x
  - クラスコンポーネント記法 (`vue-class-component`, `vue-property-decorator`)
  - 状態管理: Vuex 3.4.x
  - ルーティング: Vue Router 3.2.x
- **言語**: TypeScript / JavaScript
- **3D ライブラリ**:
  - Three.js (`three` ^0.160.0, `@pixiv/three-vrm` ^2.0.8, `@pixiv/types-vrm-0.0` ^2.0.0)
  - Babylon.js (`vue-babylonjs`, `babylon-vrm-loader`, `babylon-mtoon-material`)
- **スタイリング**: SCSS / SASS
- **ホスティング・バックエンド**: Firebase (Hosting)

---

## 4. 開発コマンド

すべてのコマンドは `app/` ディレクトリで実行します。

| コマンド | 説明 | 備考 |
| :--- | :--- | :--- |
| `npm run serve` | 開発サーバー起動 | `http://localhost:8080` で起動 |
| `npm run build` | 本番用ビルド | `dist/` に成果物が出力される |
| `npm run lint` | ESLint によるコード検証 | |
| `npm run deploy`| Firebase へのデプロイ | `npm run build && firebase deploy` |

---

## 5. コーディング規約・設計指針

1. **コンポーネント記述**:
   - 既存のコンポーネントは Vue 2 のクラスコンポーネント記法（`@Component`, `@Prop`, `@Watch` 等）で実装されています。一貫性を保ち、同等の記法を使用してください。
2. **VRM 互換性**:
   - VRM 0.x および VRM 1.0 の双方に対応できるように考慮してください（特にマテリアル、ブレンドシェイプ、アーマチュア構造のパース処理）。
3. **型安全性**:
   - TypeScript を活用し、`any` の多用を避け、適切なインターフェースや型定義を行ってください。
4. **ライブラリ追加の抑制**:
   - 不要な外部依存を追加せず、既存の Three.js や Babylon.js のエコシステム・ユーティリティを最大限活用してください。

---

## 6. Git・コミット規約

1. **作業ブランチの作成**:
   - `main` ブランチに直接変更をコミットせず、必ず機能や修正内容に応じた作業ブランチ（例: `<feature>-dev`, `<fix>-dev`）を作成して作業を行ってください。
2. **コミットの粒度**:
   - コミットは機能や修正単位で論理的にまとめてください。
3. **コミットメッセージ**:
   - コミットメッセージは**日本語**で記述してください（プレフィックス `feat:`, `fix:`, `docs:`, `refactor:` 等の使用を推奨）。

---

## 7. セキュリティ・禁止事項

> [!CAUTION]
> 以下のルールはセキュリティ保護のため厳格に遵守してください。
> - **機密ファイルへのアクセス禁止**: `.env` や `.env.key` などの機密情報ファイルには**絶対にアクセス（閲覧・編集・検索・コミット）しないでください**。
> - コミットやプルリクエストを作成する前に、API キーやシークレット情報が含まれていないか必ず確認してください。

---

## 8. エージェント行動指針

- **思考と言語**: 思考（Thinking）は英語で行い、ユーザーへの応答・レポートは**日本語**で記述すること。
- **アーティファクトの言語**: ユーザーに提示・レビューを求めるアーティファクトは**日本語**で記述すること。
- **ステップ数管理**: タスク実行中、ステップが10を超えた場合は一度処理を停止し、「完了した作業」「状況の報告」「次のアクション」を報告してユーザーの判断を仰ぐこと。
